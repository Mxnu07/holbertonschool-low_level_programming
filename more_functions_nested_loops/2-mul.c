#include <stdio.h>
#include "main.h"
/**
 * mul - miltiplies
 * @m: number to multiply
 * @b: other number to multiply
 * Return: result of multiplication
 */
int mul(int m, int b)

{
	return (m * b);
}
